---
project: 评测报告可视化与回溯平台
design_basis: Linear (dark canvas system)
adapted_for: 数据密集型内部工具 / Dashboard
ui_stack: React + Ant Design 5 (darkAlgorithm) + ECharts
---

# 设计规范 · DESIGN.md

> 本规范以 **Linear** 的暗色设计系统为基准，转译为「数据可视化 + 回溯工具」的应用版。
> Linear 原版是营销官网规范（主角是产品截图）；本平台是内部数据工具，**主角是图表与数据表格**。
> 所有 token 名沿用 Linear 命名，便于与原规范对照。本文件是后续开发（含 Agent 执行）的唯一设计依据。

---

## 0. 核心转译原则（Linear 营销 → 数据工具）

| Linear 营销原版 | 本平台应用版 |
|---|---|
| 产品截图是每个 section 的主角 | **ECharts 图表 + 数据表格**是主角 |
| 不出 light mode | 同样**只做暗色**（深色画布即留白） |
| 单一 lavender 强调色，无第二彩色 | 强调色仍单一 lavender；但**图表/语义场景允许多色**（见 §3） |
| lavender 仅用于品牌/CTA/focus/链接 | 同上，**绝不**用 lavender 做卡片或区块底色 |
| 深度靠表面阶梯 + hairline，几乎不用阴影 | 完全沿用 |
| 圆角克制（卡片 12px，按钮 8px，禁止 pill 化 CTA） | 完全沿用 |

---

## 1. 颜色 Colors

### 1.1 表面 Surface（暗色画布 + 四级阶梯）
| Token | 值 | 用途 |
|---|---|---|
| `canvas` | `#010102` | 应用主背景（near-black，带极淡蓝调，**禁止用纯黑 #000000**） |
| `surface-1` | `#0e0f12` | 卡片、面板、图表容器、表格背景 |
| `surface-2` | `#16171b` | 悬浮卡片、选中态、表格 hover 行、featured 卡片 |
| `surface-3` | `#1c1d22` | 下拉菜单、二级导航、弹层 |
| `surface-4` | `#23252a` | 最高一级抬升表面（极少用） |
| `hairline` | `#23252a` | 1px 卡片/分割线边框 |
| `hairline-strong` | `#2e3036` | 输入框、强调边框 |
| `hairline-tertiary` | `#1a1b1f` | 嵌套表面的更弱边框 |

> 层级靠"表面抬升 + 1px hairline 边框"表达，**不靠投影**。Section 之间不靠白色间隙分隔，靠 surface 抬升分隔。

### 1.2 文字 Text
| Token | 值 | 用途 |
|---|---|---|
| `ink` | `#f7f8f8` | 标题、强调正文 |
| `ink-muted` | `#d0d6e0` | 次级文字（指标说明、面板 meta） |
| `ink-subtle` | `#8a8f98` | 三级文字（表头、未选中 tab、footer） |
| `ink-tertiary` | `#62666d` | 四级文字（禁用、脚注、占位） |

### 1.3 品牌强调 Brand / Accent（单一 lavender）
| Token | 值 | 用途 |
|---|---|---|
| `primary` | `#5e6ad2` | 主按钮、品牌标、选中态、关键链接、focus 环 |
| `primary-hover` | `#828fff` | 主按钮 hover |
| `primary-focus` | `#5e69d1` | focus 环 tint（2px outline @ 50% 透明度） |
| `on-primary` | `#ffffff` | 主按钮上的文字 |

> **铁律**：lavender 只用于「品牌标 / 主 CTA / focus / 链接强调 / 当前选中」。绝不做卡片底色或区块背景。

---

## 2. 语义色 Semantic（数据工具新增）

Linear 营销版只有一个 success 绿。数据工具必须有完整语义色，用于**趋势涨跌、好坏状态、告警**。

| Token | 值 | 用途 |
|---|---|---|
| `success` | `#27a644` | 指标向好、正向趋势、达标状态 |
| `warning` | `#e2a336` | 临界、需关注 |
| `danger` | `#e5484d` | 指标恶化、负向趋势、未达标 |
| `info` | `#5e6ad2` | 中性提示（复用 lavender 色相） |

### 2.1 趋势指示规则
- 指标**上升**且方向是「越高越好」(如转化率) → `success` 绿，箭头 ↑
- 指标**下降**且方向是「越高越好」 → `danger` 红，箭头 ↓
- 「越低越好」型指标（如错误率、耗时）涨跌色**反转**
- 每个指标在模板（template）里需声明 `direction: higher_better | lower_better`，趋势染色据此判定

---

## 3. 图表配色板 Chart Palette（数据工具新增 · 核心）

Linear 在其**产品内部**使用一套多色标签板（red/orange/yellow/green/blue/purple）用于优先级与标签——我们借用同源色相，定义一套**暗背景下高可辨的多指标序列色**，用于折线图、分组柱状图、多指标对比。

### 3.1 多序列色序（按此顺序分配给第 1、2、3…个指标）
| 序号 | 名称 | 值 |
|---|---|---|
| 1 | lavender | `#5e6ad2` |
| 2 | teal | `#26b5a8` |
| 3 | amber | `#e2a336` |
| 4 | rose | `#e5556f` |
| 5 | violet | `#9b7bf0` |
| 6 | green | `#3fb950` |
| 7 | sky | `#4aa3ff` |
| 8 | coral | `#f0883e` |

> 超过 8 个指标时循环复用，但同一图表内尽量不超过 6 条序列（可读性优先）。

### 3.2 ECharts 暗色主题约定
- 图表背景：透明（继承所在 `surface-1` 卡片），**不要**给图表单独底色
- 网格线 `splitLine`：`#23252a`（= hairline），细 1px
- 坐标轴线/刻度文字：`axisLine` 隐藏或用 `hairline`；`axisLabel` 用 `ink-subtle` `#8a8f98`，12px
- 图例 legend 文字：`ink-muted` `#d0d6e0`
- tooltip：背景 `surface-3` `#1c1d22`，边框 `hairline-strong`，文字 `ink`，圆角 8px
- 折线：`smooth: false`、线宽 2px、hover 加粗到 3px；面积填充用同色 8%~12% 透明度
- 柱状图：圆角 `borderRadius: [4,4,0,0]`，柱间距适中

---

## 4. 字体 Typography

### 4.1 字体族（用开源替代 Linear 自有字体）
- **Sans（正文+标题）**：`Inter`，权重 400/500/600；fallback `-apple-system, system-ui, "Segoe UI", Roboto`
- **Mono（数值/ID/代码）**：`JetBrains Mono`，权重 400；fallback `ui-monospace, "SF Mono", Menlo`
- 数据工具约定：**所有数字、指标值、ID 一律用 Mono**，保证列对齐与可读

### 4.2 层级（应用版，比营销版更紧凑）
| Token | Size | Weight | Line H | Letter-spacing | 用途 |
|---|---|---|---|---|---|
| `display` | 32px | 600 | 1.15 | -1.0px | 页面主标题（每页仅一个） |
| `headline` | 24px | 600 | 1.20 | -0.6px | 区块标题、卡片大标题 |
| `card-title` | 18px | 500 | 1.30 | -0.3px | 卡片/面板标题 |
| `subhead` | 16px | 500 | 1.40 | -0.1px | 小标题、表单分组标题 |
| `body` | 14px | 400 | 1.50 | 0 | 默认正文（数据工具基准字号） |
| `body-sm` | 13px | 400 | 1.50 | 0 | 表格内容、次要正文 |
| `caption` | 12px | 400 | 1.40 | 0 | 表头、meta、状态、坐标轴 |
| `button` | 14px | 500 | 1.20 | 0 | 按钮文字 |
| `eyebrow` | 12px | 500 | 1.30 | +0.4px | 区块眉标（正向字距） |
| `metric-value` | 28px | 600 | 1.10 | -0.5px | 统计卡的大数字（Mono） |
| `mono` | 13px | 400 | 1.50 | 0 | 数值/ID/代码 |

> 原则：display 600 + body 400 单一声音；display 负字距，eyebrow 正字距。**不用 700+ 的超粗权重。**

---

## 5. 间距 Spacing（4px 基准）

| Token | 值 | 典型用途 |
|---|---|---|
| `xxs` | 4px | 图标与文字间隙 |
| `xs` | 8px | 紧凑内边距、标签内边距 |
| `sm` | 12px | 表单控件内边距、表格单元格纵向 |
| `md` | 16px | 卡片内容块间距 |
| `lg` | 24px | 卡片内边距、内容块之间 |
| `xl` | 32px | 面板大内边距 |
| `xxl` | 48px | 空状态、大留白区 |
| `section` | 64px | 页面主区块之间 |

- 卡片内边距：`lg` 24px（数据卡），表单密集区可用 `md` 16px
- 按钮内边距：纵 8px · 横 14px
- 输入框内边距：纵 8px · 横 12px
- 表格单元格内边距：纵 `sm` 12px · 横 `md` 16px

---

## 6. 圆角 Shapes

| Token | 值 | 用途 |
|---|---|---|
| `xs` | 4px | 状态徽章、小 chip、柱状图顶角 |
| `sm` | 6px | 内联标签 |
| `md` | 8px | **所有按钮、输入框** |
| `lg` | 12px | **所有卡片、面板、图表容器** |
| `xl` | 16px | 大型主面板（少用） |
| `pill` | 9999px | tab 切换、状态 pill |
| `full` | 9999px | 头像圆 |

> **禁止把 CTA 按钮做成 pill**。按钮一律 `md` 8px。

---

## 7. 深度与边框 Elevation

| 级别 | 处理 | 用途 |
|---|---|---|
| 0 平面 | 无边框无投影 | 正文、页面背景 |
| 1 抬升 | `surface-1` 底 + 1px `hairline` | 默认卡片、图表容器、表格 |
| 2 抬升 | `surface-2` 底 + 1px `hairline-strong` | hover 卡片、选中行、featured |
| 3 抬升 | `surface-3` 底 | 下拉、弹层、tooltip |
| 4 focus | 2px `primary-focus` outline @ 50% | 聚焦的输入框/按钮 |

- 深度靠**表面阶梯 + hairline**，**不用 drop shadow**
- 抬升面板顶边可加一道极淡白色高光（1px，~6% 白），制造"像素渲染"质感

---

## 8. 数据表格规范 Tables（数据工具新增 · 核心）

表格是本平台第二主角，规范如下：
- 容器：`surface-1` 底，圆角 `lg` 12px，1px `hairline` 边框
- 表头 `thead`：背景 `surface-2`，文字 `ink-subtle` `caption` 12px，**大写或半角**，底部 1px `hairline-strong`
- 数据行：背景透明（继承 surface-1），行间 1px `hairline-tertiary` 分割
- 斑马纹：可选，偶数行 `surface-2` 的 40% 叠加（克制）
- hover 行：整行抬升到 `surface-2`
- 选中行：左侧 2px `primary` 竖条 + `surface-2` 底
- **数值列**：右对齐，Mono 字体，`body-sm` 13px
- **文本列**：左对齐，`body-sm`
- **对比页高亮**：同一指标横向对比时，最高值文字染 `success`、最低值染 `danger`（依 `direction` 反转）
- 空状态：居中 `ink-tertiary` 文案 + 引导按钮

---

## 9. 组件 Components

### 8 按钮 Buttons
- **`button-primary`**：底 `primary`，文字 `on-primary`，`button` 字，内边距 8/14，圆角 `md`；hover→`primary-hover`，pressed→`primary-focus`
- **`button-secondary`**：底 `surface-1`，文字 `ink`，1px `hairline` 边框，其余同上（次要操作：取消、返回）
- **`button-tertiary`**：纯文字按钮，底透明，文字 `ink`
- **`button-danger`**：删除等危险操作，底透明 + `danger` 文字与边框；确认态实底 `danger`

### 卡片 Cards
- **`stat-card`**（统计卡）：`surface-1`，圆角 `lg`，内边距 `lg`；结构 = `eyebrow` 标签 + `metric-value` 大数字(Mono) + 趋势箭头(语义色) + `caption` 同比说明
- **`panel`**（图表/内容面板）：`surface-1`，圆角 `lg`，内边距 `lg`，1px `hairline`；标题用 `card-title`
- **`chart-card`**：= panel，内部嵌 ECharts，图表背景透明

### 表单 Inputs
- **`text-input` / `select`**：底 `surface-1`，文字 `ink`，圆角 `md`，内边距 8/12，1px `hairline`；focus → 2px `primary-focus` outline @ 50%
- 标签 label：`ink-muted` `caption`，位于控件上方

### Tab 与 Pill
- **`tab`**：默认 `canvas` 底 + `ink-subtle` 文字；选中 `surface-2` 底 + `ink` 文字（选中=表面抬升），圆角 `pill`，内边距 6/14
- **`status-badge`**：`surface-2` 底 + `ink-muted` 文字，圆角 `pill`，内边距 2/8，`caption` 字；语义态用对应语义色文字

### 导航 Navigation
- **`top-nav`**：sticky，`canvas` 底，高 56px；左侧 wordmark/平台名，中部主导航（Dashboard / 上传 / 对比），右侧操作按钮
- **`side-nav`**（如用侧栏）：`canvas` 底，选中项 `surface-2` 底 + 左 2px `primary` 竖条

---

## 10. Ant Design 5 主题映射

在 `ConfigProvider` 用 `theme.darkAlgorithm`，并覆盖以下 token，使 AntD 组件落到本规范：

```ts
import { theme } from 'antd';

const evalTheme = {
  algorithm: theme.darkAlgorithm,
  token: {
    colorPrimary: '#5e6ad2',
    colorInfo: '#5e6ad2',
    colorSuccess: '#27a644',
    colorWarning: '#e2a336',
    colorError: '#e5484d',

    colorBgBase: '#010102',          // canvas
    colorBgContainer: '#0e0f12',     // surface-1
    colorBgElevated: '#1c1d22',      // surface-3 (弹层/下拉)
    colorBorder: '#23252a',          // hairline
    colorBorderSecondary: '#1a1b1f', // hairline-tertiary

    colorText: '#f7f8f8',            // ink
    colorTextSecondary: '#d0d6e0',   // ink-muted
    colorTextTertiary: '#8a8f98',    // ink-subtle
    colorTextQuaternary: '#62666d',  // ink-tertiary

    fontFamily: 'Inter, -apple-system, system-ui, "Segoe UI", Roboto, sans-serif',
    fontFamilyCode: '"JetBrains Mono", ui-monospace, "SF Mono", Menlo, monospace',
    fontSize: 14,

    borderRadius: 8,        // 按钮/输入框 md
    borderRadiusLG: 12,     // 卡片 lg

    controlHeight: 36,      // 按钮/输入默认高
    wireframe: false,
  },
  components: {
    Card:   { borderRadiusLG: 12, paddingLG: 24 },
    Button: { borderRadius: 8, controlHeight: 36, fontWeight: 500 },
    Table:  { headerBg: '#16171b', headerColor: '#8a8f98', rowHoverBg: '#16171b' },
    Tabs:   { itemSelectedColor: '#f7f8f8' },
  },
};
```

---

## 11. 四个页面的应用指引

### 11.1 Dashboard（首页）
- 顶部一行 `stat-card`（报告总数 / 业务标签数 / 最近上传），数字用 `metric-value` Mono
- 下方报告列表用本规范 §8 表格；右上角主操作 `button-primary`「上传报告」
- 业务标签筛选用 `tab` pill 行

### 11.2 Upload（上传页）
- 拖拽区：`surface-1` 虚线 `hairline-strong` 边框，圆角 `lg`，居中 `ink-subtle` 提示
- 上传后列预览用紧凑表格（`body-sm`）
- Schema 映射表单：`select` 选「日期列」「指标列(多选)」「维度列」；每个指标设 `direction`
- 底部 `button-primary`「确认解析」+ `button-secondary`「取消」

### 11.3 ReportDetail（报告详情）
- 顶部信息条：报告名(`headline`) + 标签(`status-badge`) + 日期/行数(`caption`)
- `chart-card` 展示分组柱状图（多指标用 §3 色序）
- 下方数据表（§8）
- 右上 `button-secondary`「加入对比」

### 11.4 Compare（对比页）
- 左栏：报告多选器（按标签过滤），选中项 `surface-2` + 左 `primary` 竖条
- 右栏上：指标选择 `tab` + 折线图（`chart-card`，按时间排序，多指标 §3 色序）
- 右栏下：横向对比表，**最高值染 `success`、最低值染 `danger`**（依指标 `direction` 反转）

---

## 12. Do's / Don'ts

### Do
- 画布锚定 `#010102`，淡蓝调是有意为之
- lavender 只用于：品牌标 / 主 CTA / focus / 链接 / 当前选中
- 用四级表面阶梯表达层级，不跳级
- 所有数字、指标值、ID 用 Mono，右对齐
- 图表多序列按 §3 色序分配；图表背景透明
- display 600 + body 400，display 用负字距

### Don't
- 不做 light mode
- 不用 lavender 当卡片/区块底色
- 不用纯黑 `#000000` 当画布
- 不给 CTA 按钮做 pill 圆角
- 不用 drop shadow 制造层级（用表面+hairline）
- 不在一张图里堆超过 6 条序列
- 图表不要单独底色（继承卡片 surface-1）

---

## 13. 响应式

| 断点 | 宽度 | 变化 |
|---|---|---|
| Desktop-XL | ≥1440px | 默认布局，统计卡 4-up |
| Desktop | 1280px | 统计卡 3~4-up，表格全宽 |
| Tablet | 1024px | 统计卡 2-up；对比页左右栏改上下 |
| Mobile | ≤768px | 单列；表格横向滚动；导航收起为抽屉 |

- 最大内容宽度约 1280px，居中
- 触控目标 ≥40px；输入/按钮触控态 ≥44px
- 图表保持宽高比，移动端可缩短高度但不裁剪

---

*本规范为开发唯一设计依据。新增组件请按 token 体系扩展，并在 §9 追加条目。*
